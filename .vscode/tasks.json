{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Engine",
            "windows": {
                "command": "${workspaceFolder}/b.bat"
            },
            "linux": {
                "command": "${workspaceFolder}/b.sh"
            },
            "options": {
                "cwd": "${workspaceFolder}/"
            },
            "group": "build",
            "detail": "Task generated by Debugger."
        }
    ]
}