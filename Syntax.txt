$import "Basic"


// This is a comment

func name(param: type) -> ret_type
{
	value: u32 = 0;

	value += 2;
	++value;
	value++;

	arr:[] i32 = { 1, 2, 3}
	arr:[4096]u32 = {}; // 0 init

	pointer:* u32 = &value;
	pointer = arr;

	some_var: u32 = 10.456;

	arr[2] = 0;


	-> value;
}


func main() -> void
{
	a: param = param(1, 2);

	print("oy {ret_type}", name(a))
	other_variable: i32 = name(a);
	Basic.print("ok");
}