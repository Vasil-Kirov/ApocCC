
func $intrinsic var_arg_start(list:** Any);

func _apoc_init() -> i32
{
	// if and for are completely broken
	builder := init_builder("my text");
	builder += "more text";
	my_str := builder_to_string(builder);
	print(my_str);
	-> 0;
}

