
global_var := 10;

func _apoc_init() -> i32
{
	a := My_Nums {40, 20, 10, 5};
	c := $size My_Nums;
	a.b = change_a(a.b);
	if a.b == 3
	{
		puts("a");
		-> 0;
	}
	else if a.b == 2
		-> 1;
	else
		-> 0;
	-> $size My_Nums;
}

func puts(str:* u8) ->;

func change_a(a: i32) -> i32
{
	-> a / 2;
}


struct My_Nums
{
	e: i8;	// 0 - 1
	a: i32; // 4 - 7
	c: i64; // 8 - 15
	b: i32; // 16 - 19
}           // allign to size of i64 (8) = 24
